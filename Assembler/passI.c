#include <stdio.h>
#include <stdlib.h>

void main()
{ 
	FILE *fp;
	char label[10], opcode[10], operand[10];

	int locctr, i;

	fp = fopen("assembly.txt", "r");

	while(fscanf(fp, "%s", opcode) >= )
	{ 
	}
}
